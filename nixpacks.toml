[provider]
id = "python"

[python]
version = "3.11"

[phases.setup]
cmds = [
    "apt-get update && apt-get install -y ffmpeg && apt-get clean",
]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
]

[start]
cmd = "python bot.py"